# CNHK MCP 工具使用指南

## 概述

这是一个用于WorldQuant BRAIN量化交易平台的MCP（Model Context Protocol）工具集，提供了以下核心功能：

1. **BRAIN平台集成** - 与WorldQuant BRAIN API交互
2. **Alpha分析与优化** - 分析、解释和改进交易信号（Alpha）
3. **学术研究工具** - 通过arXiv API搜索金融研究论文
4. **论坛数据提取** - 从BRAIN社区论坛获取知识

---

## 目录结构

```
cnhkmcp/untracked/
├── platform_functions.py          # BRAIN平台MCP服务器主程序
├── arxiv_api.py                   # arXiv论文搜索工具
├── forum_functions.py             # 论坛功能函数
├── user_config.json               # 用户配置文件（需要填写）
├── sample_mcp_config.json         # MCP服务器配置示例
├── 示例参考文档_BRAIN_Alpha_Test_Requirements_and_Tips.md
├── 示例工作流_Alpha_explaination_workflow.md
├── 示例工作流_BRAIN_Alpha_Improvement_Workflow.md
├── 示例工作流_BRAIN_6_Tips_Datafield_Exploration_Guide.md
├── 示例工作流_daily_report_workflow.md
├── 示例工作流_Dataset_Exploration_Expert_Manual.md
└── arXiv_API_Tool_Manual.md
```

---

## 一、环境配置

### 1.1 安装依赖

首先运行安装脚本：

```bash
python "配置前运行我_安装必要依赖包.py"
```

或者手动安装：

```bash
pip install requests beautifulsoup4 selenium webdriver-manager
```

### 1.2 配置用户凭证

编辑 `user_config.json` 文件，填入你的BRAIN平台账号信息：

```json
{
  "credentials": {
    "email": "你的邮箱@example.com",
    "password": "你的密码"
  },
  "api_settings": {
    "base_url": "https://api.worldquantbrain.com",
    "timeout": 30,
    "retry_attempts": 3
  },
  "simulation_defaults": {
    "type": "REGULAR",
    "instrument_type": "EQUITY",
    "region": "USA",
    "universe": "TOP3000",
    "delay": 1,
    "decay": 0,
    "neutralization": "NONE",
    "visualization": true
  }
}
```

### 1.3 配置MCP服务器

将 `sample_mcp_config.json` 的内容添加到你的MCP客户端配置中（如Claude Desktop、Cursor等）。

---

## 二、arXiv论文搜索工具

### 2.1 功能介绍

用于搜索和下载学术论文，帮助获取量化金融研究灵感。

### 2.2 使用方法

#### 命令行模式

```bash
# 基础搜索
python arxiv_api.py "machine learning finance"

# 指定结果数量
python arxiv_api.py "quantitative trading momentum" -n 10

# 搜索并下载第一个结果
python arxiv_api.py "portfolio optimization" -d

# 交互模式
python arxiv_api.py -i
```

#### 交互模式命令

```
📚 arxiv> search quantitative finance 5
📚 arxiv> download 2502.05218v1
📚 arxiv> help
📚 arxiv> quit
```

#### Python脚本调用

```python
from arxiv_api import search_and_download, download_paper, get_paper_metadata

# 搜索论文
search_and_download("factor investing", max_results=5)

# 下载特定论文（自动重命名为论文标题）
download_paper("2502.05218v1")

# 获取论文元数据
paper_info = get_paper_metadata("2502.05218v1")
```

### 2.3 高级搜索语法

```bash
# 按作者搜索
python arxiv_api.py "au:Yann LeCun"

# 按标题搜索
python arxiv_api.py "ti:deep learning"

# 按类别搜索
python arxiv_api.py "cat:q-fin.ST"  # 量化金融-统计金融

# 复合查询
python arxiv_api.py "machine learning AND finance AND submittedDate:[20230101 TO 20231231]"
```

---

## 三、BRAIN Alpha分析工作流

### 3.1 Alpha表达式解释工作流

**目标**：分析和理解一个Alpha表达式的逻辑和策略

#### 步骤1：分解表达式

例如：`quantile(ts_regression(oth423_find,group_mean(oth423_find,vec_max(shrt3_bar),country),90))`

- **数据字段**：`oth423_find`, `shrt3_bar`
- **算子**：`quantile`, `ts_regression`, `group_mean`, `vec_max`

#### 步骤2：分析数据字段

使用MCP工具查询数据字段信息：

```xml
<use_mcp_tool>
<server_name>brain-platform-mcp</server_name>
<tool_name>get_datafields</tool_name>
<arguments>
{
  "instrument_type": "EQUITY",
  "region": "ASI",
  "delay": 1,
  "universe": "MINVOL1M",
  "data_type": "VECTOR",
  "search": "shrt3_bar"
}
</arguments>
</use_mcp_tool>
```

#### 步骤3：理解算子

使用MCP工具获取算子列表：

```xml
<use_mcp_tool>
<server_name>brain-platform-mcp</server_name>
<tool_name>get_operators</tool_name>
<arguments>{}</arguments>
</use_mcp_tool>
```

#### 步骤4：外部研究

使用arXiv工具搜索相关学术论文：

```bash
python arxiv_api.py "short interest fundamental analysis" -n 10
```

#### 步骤5：综合解释

按照以下模板整理分析结果：

```
## Alpha策略概述
[高层级策略总结]

## 数据字段分析
- oth423_find: [描述]
- shrt3_bar: [描述]

## 算子逻辑
1. vec_max(shrt3_bar): [解释]
2. group_mean(...): [解释]
3. ts_regression(...): [解释]
4. quantile(...): [解释]

## 策略灵感来源
[参考的学术论文或研究]
```

---

### 3.2 Alpha改进工作流

**目标**：系统化地改进Alpha以通过平台测试

#### 循环流程（每轮30-60分钟）

**步骤1：收集Alpha信息（5-10分钟）**
- 获取Alpha详情（表达式、设置、指标）
- 运行提交检查和相关性检查
- 分析PnL趋势和年度统计

**步骤2：评估核心数据字段（5-10分钟）**
- 使用6种评估方法分析数据字段特性：
  1. 基础覆盖率
  2. 非零覆盖率
  3. 更新频率
  4. 数据边界
  5. 中心趋势
  6. 分布特征

**步骤3：提出改进方案（10-15分钟）**
- 查阅平台文档和社区示例
- 使用arXiv搜索研究论文获取灵感
- 设计4-6个改进变体

**步骤4：模拟测试（10-20分钟）**
- 运行多表达式模拟
- 按Fitness/Sharpe排序结果
- 记录指标变化

**步骤5：验证并迭代（5-10分钟）**
- 运行提交/相关性检查
- 如果失败，返回步骤3
- 如果通过，提交Alpha

#### Alpha测试要求速查表

| 测试项 | 要求 | 改进技巧 |
|--------|------|----------|
| **Fitness** | >1.3 (Delay-0) 或 >1 (Delay-1) | 平衡Sharpe、Returns和Turnover |
| **Sharpe** | >2 (Delay-0) 或 >1.25 (Delay-1) | 使用衰减函数平滑信号 |
| **Turnover** | 1% < x < 70% | 避免过度交易 |
| **权重测试** | 最大权重<10% | 使用neutralize分散权重 |
| **子宇宙测试** | Sharpe >= 0.75 * sqrt(比例) | 避免依赖小盘股 |
| **自相关** | <0.7 | 提交多样化的Alpha想法 |

---

## 四、数据字段探索指南

### 4.1 六种评估方法

使用中性化设置（neutralization="NONE", decay=0）模拟以下表达式：

1. **基础覆盖率**
   ```
   datafield
   ```
   含义：(多头+空头数量) / 宇宙大小

2. **非零覆盖率**
   ```
   datafield != 0 ? 1 : 0
   ```
   含义：实际有意义的数据点

3. **更新频率**
   ```
   ts_std_dev(datafield, 5) != 0 ? 1 : 0    # 日频
   ts_std_dev(datafield, 22) != 0 ? 1 : 0   # 月频
   ts_std_dev(datafield, 66) != 0 ? 1 : 0   # 季频
   ```

4. **数据边界**
   ```
   abs(datafield) > 100
   abs(datafield) > 1000
   ```

5. **中心趋势**
   ```
   ts_median(datafield, 1000) > X
   ```

6. **分布特征**
   ```
   0.25 < scale_down(datafield) && scale_down(datafield) < 0.75
   ```

### 4.2 应用洞察

根据评估结果选择合适的算子：
- **稀疏数据** → 使用 `ts_backfill` 填充
- **单位问题** → 使用 `scale` 标准化
- **季度更新** → 使用长窗口（如252天）
- **高波动** → 使用 `ts_decay_linear` 平滑

---

## 五、常用算子速查

### 时间序列算子
- `ts_rank(x, d)`: 过去d天x的排名
- `ts_mean(x, d)`: 过去d天x的均值
- `ts_std_dev(x, d)`: 过去d天x的标准差
- `ts_delta(x, d)`: x - d天前的x
- `ts_corr(x, y, d)`: x和y过去d天的相关系数
- `ts_regression(x, y, d)`: x对y的回归系数
- `ts_decay_linear(x, d)`: 线性衰减加权平均
- `ts_backfill(x)`: 向后填充NaN值

### 向量算子
- `vec_max(v)`: 向量最大值
- `vec_min(v)`: 向量最小值
- `vec_mean(v)`: 向量均值
- `vec_sum(v)`: 向量求和

### 分组算子
- `group_rank(x, g)`: 组内排名
- `group_mean(x, g)`: 组内均值
- `group_zscore(x, g)`: 组内Z-score标准化

### 标准化算子
- `rank(x)`: 横截面排名（0-1）
- `scale(x)`: 标准化为单位方差
- `scale_down(x)`: 缩放到0-1范围
- `neutralize(x, g)`: 中性化（去除组均值）

### 条件算子
- `x > y ? a : b`: 条件赋值
- `abs(x)`: 绝对值
- `sign(x)`: 符号函数

---

## 六、常见问题解决

### 6.1 SSL证书错误

如果运行Python脚本时出现 `CERTIFICATE_VERIFY_FAILED` 错误：

```bash
# 方法1：使用--trusted-host
pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org requests

# 方法2：在脚本中禁用SSL验证（仅用于测试）
import ssl
ssl._create_default_https_context = ssl._create_unverified_context
```

### 6.2 认证失败

检查 `user_config.json` 中的邮箱和密码是否正确。

### 6.3 模拟超时

增加 `api_settings.timeout` 值或减少同时模拟的表达式数量。

### 6.4 负Sharpe处理

对于非中国市场Alpha，如果Sharpe为负，添加负号翻转：
```
-original_expression
```

---

## 七、最佳实践

1. **从简单开始**：先用基础算子（如 `ts_rank`, `ts_corr`）测试想法
2. **单一数据集**：优先使用ATOM（单一数据集）Alpha以获得更宽松的测试要求
3. **迭代改进**：每次只做小改动，记录指标变化
4. **学术支撑**：使用arXiv搜索论文为策略提供理论基础
5. **多样化**：避免提交高相关的Alpha（自相关<0.7）
6. **年度统计**：确保至少5年有数据，避免稀疏覆盖

---

## 八、快速开始示例

### 示例1：搜索研究论文

```bash
# 搜索动量策略相关论文
python arxiv_api.py "momentum trading strategy quantitative finance" -n 5

# 下载感兴趣的论文
python arxiv_api.py -i
📚 arxiv> download 2401.12345v1
```

### 示例2：分析Alpha表达式

假设有一个Alpha：`ts_rank(close, 20)`

分析步骤：
1. `close`: 收盘价数据字段
2. `ts_rank(x, 20)`: 过去20天的排名
3. 策略逻辑：基于20日价格排名的动量信号

改进建议：
```python
# 添加市场中性化
neutralize(ts_rank(close, 20), "MARKET")

# 添加线性衰减
ts_decay_linear(ts_rank(close, 20), 5)

# 添加行业分组
group_zscore(ts_rank(close, 20), "INDUSTRY")
```

### 示例3：运行模拟

使用MCP工具运行模拟：

```xml
<use_mcp_tool>
<server_name>brain-platform-mcp</server_name>
<tool_name>create_simulation</tool_name>
<arguments>
{
  "alpha": "ts_rank(close, 20)",
  "settings": {
    "region": "USA",
    "universe": "TOP3000",
    "delay": 1
  }
}
</arguments>
</use_mcp_tool>
```

---

## 九、相关资源

- **BRAIN平台文档**: https://platform.worldquantbrain.com/learn
- **arXiv量化金融**: https://arxiv.org/list/q-fin/recent
- **算子完整列表**: 使用 `get_operators` MCP工具获取
- **社区论坛**: https://support.worldquantbrain.com

---

*文档最后更新：2026年3月22日*