# 回测监控报告 — 2026-07-25 00:22 (GMT+8，周六)

> 用户手动发起（V33 HKG PPA 进度汇报框架）｜框架：进程盘点 → 并发模型 → 效率结论 → ETA 分析

## 一、进程盘点（Python 全量枚举 + 分类）

| 类别 | 任务 | 进度 | 状态 |
|---|---|---|---|
| WQ 挖掘 | `scan_v44_insider_feats.py`（主账号，multi-sim） | 88/200 (44%) | **运行中** |
| WQ 挖掘 | `scan_v45_tri_insider_feats.py`（主账号，multi-sim） | 16/320 (5%) | **运行中** |
| 已完成 | `scan_v43_event_relation.py` | 200/200 (100%) | 结束 @22:52:25，0 候选 |
| 已停止 | `tabbit_option9.py` | CSV 434 done | 停止 @23:01:07，0 候选 |
| 基础设施 | ~20 个 | — | idle |

- 全量 `python.exe` 共 **22** 个（较 23:36 的 19 增加 3：V44 + V45 + 可能的启动器）。**当前 2 个活跃 WQ 挖掘任务（V44、V45）**，其余为空闲基础设施。
- 注：`tasklist` 仅显示进程名不含命令行，且 wmic 对这批进程 `CommandLine` 返回空，故无法直接取 PID；进度以 checkpoint + 进度日志为准（两者均含实时 `done`、无 `finish` 事件，确证在跑）。
- V44 脚本创建于 22:54（紧接 V43 收尾后），进度日志 `v44_insider_feats_progress_20260724_225455.log`；V45 进度日志 `v45_tri_progress_20260725_000853.log`。

## 二、并发模型（⚠ 关键风险）

| 任务 | 并发模型 | 批次 | 实测吞吐 | 账号 | 槽位风险 |
|---|---|---|---|---|---|
| V44 | multi-sim（单 POST 提 N 条 list，1 槽并行） | 推测 BATCH=8 | ≈60 α/hr（avg 59.93 s/step） | 主账号 | **与 V45 同账号争用** |
| V45 | multi-sim（同上） | 推测 BATCH=8 | ≈80 α/hr（avg 45.39 s/step，早期波动大） | 主账号 | **与 V44 同账号争用** |

- **核心风险**：V44 与 V45 均为 `scan_v*` 主账号脚本（沿用 `.env` 主账号 `WqApiSimple`），**疑似在同一主账号上并发提交 multi-sim**。WQ 单账号的模拟槽位为串行资源，两个并发扫描会互相排队，导致：
  1. 各自吞吐低于单任务基准（V43 单独跑时 ≈74–78 α/hr；V44 实测 60 α/hr 已偏低，疑似受争用拖累）；
  2. 重叠提交可能触发平台速率限制 / 提交错误 / 浪费每周提交额度。
- **待核实**：V45 是否确实使用主账号？若是，强烈建议改为**串行执行**（先跑完 V44 再跑 V45），或将 V45 切到教程账号（brain.txt）以真正并行。
- V44 启动于 22:54，V45 启动于 ~00:08，二者时间重叠。

## 三、效率结论

1. **吞吐受同账号并发拖累**：若 V44/V45 共享主账号槽位，实际总吞吐 ≠ 两者之和，而是被串行化；单任务基准（multi(8)=86.1 α/hr）无法在并发下复现。需先解决账号隔离或串行化。
2. **信号方向出现边际改善，但距闸门仍远**：V44 最佳 Sharpe **0.60**、V45 最佳 **0.64**，均高于 V43 的 0.47（event_relation 死胡同），说明 **insider_feats 方向略优**；但两者仍远低于 1.25 闸门，**当前 0 候选**。
3. **绝对瓶颈仍是信号发现**：V31–V45 + tabbit 累计 ≈2400+ alpha，仅 V39b（YPgAa3WR）1 条通过全部门槛。V44/V45 的 insider_feats 是相对更有希望的方向，但仍未突破。

## 四、ETA 分析（必填）

### V44 insider_feats
- **完成/总数**：88 / 200（44%）
- **已运行**：约 88 min（自 22:54）
- **剩余**：112 变体
- **吞吐**：≈60 α/hr（avg 59.93 s/step）
- **墙钟 ETA**：112 × 59.93s ≈ 6712s ≈ 1h52m → 日志自投影 **~2026-07-25 02:14:42**（置信度：中高；multi-sim 节奏确定，但同账号争用可能拉长）。
- **后处理**：待确认是否 `NO_SUBMIT`；当前 0 候选 → 即便有后处理阶段也无候选可处理。
- **置信度**：中高。

### V45 tri_insider_feats
- **完成/总数**：16 / 320（5%）
- **已运行**：约 14 min（自 ~00:08）
- **剩余**：304 变体
- **吞吐**：≈80 α/hr（avg 45.39 s/step，早期样本少、波动大）
- **墙钟 ETA**：304 × 45.39s ≈ 13800s ≈ 3h50m → 日志自投影 **~2026-07-25 04:10:57**（置信度：中；仅 5% 进度，总变体 320 偏大，争用/波动会显著拉动 ETA）。
- **后处理**：待确认；当前 0 候选。
- **置信度**：中（早期，ETA 易变）。

### V43 / tabbit
- V43：已完成（22:52:25），无 ETA。
- tabbit：已停止（23:01:07），无 ETA。

### 关键提醒
- V44 与 V45 **ETA 非独立**：若同账号争用槽位，并发会同时拉长两者墙钟时间；串行化后总完成时刻可能更早且更可控。

## 五、综合建议（待用户决策）

1. **🚩 立即核实并化解同账号并发风险**：确认 V45 账号归属。若与主账号相同，**改为串行**（V44 跑完再启 V45），或把 V45 切到教程账号 `brain.txt` 实现真正并行——否则两任务相互拖累且浪费额度。
2. **insider_feats 是当前最有希望方向**：V44/V45 最佳 Sharpe 0.60/0.64 已优于此前所有死胡同方向；建议 V44 收尾后重点看其候选分布，必要时围绕高分变体做 decay/中性化扩展。
3. **V33 HKG**：自 07-23 14:20 暂停，未动。

---
*数据源：V44 `results/v44_insider_feats_checkpoint.json` + `v44_insider_feats_progress_20260724_225455.log`；V45 `results/v45_tri_insider_feats_checkpoint.json` + `v45_tri_progress_20260725_000853.log`；`tasklist`/`wmic` 进程枚举。*
