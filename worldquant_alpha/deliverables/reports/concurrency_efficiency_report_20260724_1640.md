# WQ PPA 挖掘任务 · 并发与平台效率评估报告

- 生成时间：2026-07-24 16:40 (GMT+8)
- 自动化：V33 HKG PPA 进度汇报（本轮聚焦于用户要求的"并发量 + 是否达平台最大效率"评估）
- 数据源：`bench_v34_sim_speed_20260723_171444.json`、`results/v41_earn_risk_checkpoint.json`、各 `scan_v*.py` 源码、实时进程列表

---

## 0. 执行摘要

- **当前活跃作业：仅 V41 earn_risk**（PID 22428）。进度 88/180 变体（49%），0 候选，实时吞吐 **86.6 α/hr**，ETA ≈ 17:29。
- **V33 HKG**：自 07-23 14:20 用户手动暂停后未再启动。
- **V39b 候选** `gz_t2_b66z189_TOP3000_d3_SEC_t1` 已于 07-24 成功提交并 **ACTIVE**（系统内唯一上线 alpha）。
- **并发模型**：现行任务统一采用 **multi-sim**，批大小 `BATCH_SIZE=8`（一次 POST 提交含 8 条表达式的 list，平台用 1 个槽位并行跑 8 个子任务）+ 批间冷却 40–45s。
- **是否达平台最大效率**：**当前未达平台理论上限，但已显著优于逐条提交（1.59×），落在该并发模型的设计最优区。** 两个信息缺口使"已达上限"无法被证实：(a) 批大小 >8 从未基准测试；(b) 系统内长期仅 1 个并发作业在跑，并发槽位闲置。

---

## 1. 各任务并发配置与状态

| 任务 | 状态 | 并发模型 | 批大小 | 冷却 | 进度 | 候选 |
|------|------|----------|--------|------|------|------|
| **V41 earn_risk** | 运行中 | multi-sim | 8 | 45s | 88/180 (49%) | 0 |
| V40 cre_exposure | 已完成 (07-24 15:23) | multi-sim | 8 | 45s | 200/200 | 0 |
| V39b sub_micro | 已提交 | multi-sim | 8 | 40s | 240/240 | 1 (ACTIVE) |
| V34 insider_matrix | 已完成/弃用 | multi-sim (修复后) | 8 | 45s | 324/324 | 0 |
| V33 HKG v2 | 暂停 (07-23 14:20) | ThreadPool 5 workers（旧范式） | — | — | 55/1100 | 0 |
| V33 HKG v3 | 未启用 | multi-sim | 10 | — | — | — |

要点：

- 现行纪律（`multi_sim.py`）要求每批填满 8 条、用 1 个 POST 提交 list，**禁止用 ThreadPool 对每条 alpha 单独 `create_simulation` 冒充 multi**。V33 v2 的 5-worker ThreadPool 属被弃用的旧范式，且已暂停。
- 全部现行任务吞吐上限由公式 `批大小 / (批 wall + 冷却)` 决定；实测由基准与 V41 实时数据共同约束。

---

## 2. 效率基准（bench_v34_sim_speed_20260723_171444.json）

| 模式 | N | wall(s) | s/α | α/hr | 结论 |
|------|---|---------|------|------|------|
| **multi-sim (BATCH=8)** | 8 | 334.6 | 41.8 | **86.1** | 基准最优 |
| single（逐条提交） | 8 | 530.4 | 66.3 | 54.3 | 慢 1.59× |

`speedup = 86.1 / 54.3 = 1.59×`。即当前 multi-sim 比逐条提交快约 **59%**，且更省请求数（8 条 / 1 POST vs 8 条 / 8 POST）。

---

## 3. 当前实际吞吐 vs 基准

V41 实时测算：elapsed 61 min，done 88 → **86.6 α/hr**，与基准 86.1 几乎吻合。结论：当前作业已稳定跑在 multi-sim 设计吞吐上，**无异常退化**。

---

## 4. 是否达到平台最大效率？—— 评估与信息缺口

**结论先行：当前未达平台理论上限，但已落在该并发模型下的设计最优区。**

证据与缺口：

1. **批大小维度**：基准只对比了 `multi(8)` vs `single`，**未测试 `multi(16)` / `multi(24)`**。`multi_sim.py` 顶部注释仅说"推荐填满 8 条"，未声明 8 是平台硬上限。现有数据**无法排除**"更大批次仍可提高 α/hr"的可能。
2. **作业并行维度**：系统内**长期只有 1 个 multi-sim 作业在跑**（当前仅 V41）。WQ 平台允许一定数量的并发 simulation 槽位；若同时拉起 2–3 个不同数据集的扫描任务，总吞吐可超过 86 α/hr——前提是平台并发槽位充足且不被 429 限流。
3. **冷却维度**：批间冷却 40–45s 是保守值（防 429）。若平台实际容忍更短冷却，可进一步压缩空隙。

因此，"是否达平台最大效率"在现有证据下**无法给出肯定结论**——我们证明的是"8 条/批的 multi-sim 显著优于逐条"，而非"8 是平台绝对最优批次"。

---

## 5. 建议（待用户决策）

**压榨并发的可选动作：**

- **A. 补一次基准**：跑 `multi(16)` 与 `multi(24)`，对比 wall / α/hr，确认 8 是否吞吐量拐点。风险：可能触发 429，属可选验证。
- **B. 并行多任务**：在 V41 之外拉起 1–2 个不同数据集 / 范式的扫描（如 V42 新范式），利用闲置并发槽位提升总吞吐。

**但需清醒认知（关键约束）：**

当前瓶颈是 **信号发现（0 候选），而非计算吞吐**。V31–V41 累计约 1460+ 变体零平台通过，insider 家族被子宇宙 Sharpe 跷跷板结构性卡死。在找到可过闸门的信号之前，把吞吐从 86 提到 150 α/hr 只意味着"更快地挖出 0 候选"。**优先项应是信号范式转向，而非并发调优。**

---

## 6. 待办

- V41 跑完（~17:29）若仍 0 候选 → 复盘策略方向（换数据集 / 换范式 / 回到 V39b 低自相关风格扩展）。
- V33 HKG 维持暂停，待用户重启决策。
- 本周提交额度：WQ API 不暴露（`/users/self` 无 quota 字段、`/submission-limit` 404）；仅 Web UI Submissions 面板可见。V39b 单发成功后平台未报错，代表本周额度未耗尽。
