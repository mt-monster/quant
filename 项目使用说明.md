# WorldQuant Alpha策略生成和回测工具 - 使用说明

## 项目概述

这是一个用于自动生成、回测和分析WorldQuant Alpha策略的专业工具，通过wd_lib库与WorldQuant Brain平台集成，提供完整的Alpha因子生命周期管理。

## 主要功能

- **Alpha策略自动生成**: 基于模板和数据字段批量生成Alpha表达式
- **批量回测**: 支持并发回测大量Alpha策略
- **性能分析**: 分析Alpha策略的Sharpe比率、IR、换手率等指标
- **高质量筛选**: 自动筛选符合标准的优质Alpha策略
- **三阶Pipeline**: 支持一阶、二阶、三阶Alpha生成流程
- **断点续传**: 支持长时间运行任务的断点恢复

## 系统要求

- Python 3.7+
- MySQL数据库
- WorldQuant账号和API访问权限

## 安装步骤

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

复制并编辑配置文件：

```bash
cp worldquant_alpha/.env.example worldquant_alpha/.env
```

编辑 `.env` 文件，填入必要配置：

```env
# WorldQuant凭据
WQ_USERNAME=your_username
WQ_PASSWORD=your_password

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=worldquant_alpha

# 邮件通知配置（可选）
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_email
SMTP_PASSWORD=your_email_password
EMAIL_FROM=alpha@example.com
EMAIL_TO=user@example.com

# 日志级别
LOG_LEVEL=INFO
```

## 核心命令

### 1. 系统初始化

```bash
cd worldquant_alpha
python -m main init
```

### 2. 获取数据字段

```bash
python -m main fetch --dataset fundamental6
```

### 3. 生成Alpha表达式

```bash
# 使用默认模板
python -m main generate --template 0 --limit 10

# 使用1阶工厂函数
python -m main generate --template 1 --limit 50
```

### 4. 运行回测

```bash
# 从数据库回测
python -m main backtest --from_db --limit 10

# 增强版回测命令
python -m main backtest-db --status pending --max_workers 4
```

### 5. 分析结果

```bash
python -m main analyze --ir_threshold 0.1 --limit 100
```

### 6. 完整流程

```bash
# 使用预设配置
python -m main pipeline --config analyst10_eur

# 自定义参数
python -m main pipeline --region EUR --universe TOP2500 --limit 50
```

### 7. 三阶Pipeline

```bash
# 运行三阶生成
python -m main third_order run

# 使用指定配置
python -m main third_order run --config third_order_aggressive.yaml

# 查看状态
python -m main third_order status

# 恢复运行
python -m main third_order resume
```

## 预设配置

项目提供了多种预设配置：

- `analyst10_eur`: EUR地区分析师预期数据
- `analyst10_usa`: USA地区分析师预期数据
- `sal_eur/usa`: 销售额类模板
- `ebi_eur/usa`: EBIT类模板
- `revise_eur/usa`: 修正类模板（低换手率）
- `innovation_eur/usa`: 创新修正类模板
- `high_turnover_eur`: 高换手率策略
- `low_turnover_usa`: 低换手率策略

## 配置参数说明

### 回测设置
- `--region`: 地区 (USA/EUR/CHN等)
- `--universe`: 股票池 (TOP3000/TOP2500等)
- `--delay`: 延迟天数 (0或1)
- `--decay`: 衰减天数
- `--neutralization`: 中性化方式 (MARKET/SECTOR/INDUSTRY/SUBINDUSTRY)
- `--truncation`: 截断比例 (0.01-0.10)
- `--sharpe_threshold`: Sharpe比率阈值
- `--ir_threshold`: 信息比率阈值

### 并发设置
- `--max_workers`: 并发线程数 (1-10)
- `--limit_per_template`: 每模板生成数量

## 项目结构

```
worldquant_alpha/
├── main.py                 # 主程序入口
├── alpha_generator.py      # Alpha生成器
├── backtest.py            # 回测功能
├── database.py            # 数据库操作
├── notification.py        # 通知功能
├── wd_lib_wrapper.py      # WorldQuant API封装
├── pipeline/              # 三阶Pipeline模块
│   ├── engine.py          # Pipeline引擎
│   ├── stages/            # 各阶段实现
│   ├── core/              # 核心组件
│   └── config/            # 配置管理
├── configs/               # 配置文件
├── data/                  # 数据字段文件
├── examples/              # 示例代码
└── wd_lib/                # WorldQuant库封装
```

## 使用示例

### 完整工作流程

```bash
# 1. 初始化系统
python -m main init

# 2. 获取数据字段
python -m main fetch --dataset fundamental6

# 3. 生成Alpha表达式
python -m main generate --template 0 --limit 20

# 4. 运行回测
python -m main backtest --from_db --limit 20

# 5. 分析结果
python -m main analyze --ir_threshold 0.15
```

### 批量处理

```bash
# 批量生成和回测
python -m main pipeline --config analyst10_eur --limit_per_template 50 --max_workers 6
```

### 三阶生成

```bash
# 运行三阶Pipeline
python -m main third_order run --config third_order_default.yaml

# 查看执行状态
python -m main third_order status
```

## 常见问题

### 1. 无法连接WorldQuant API
- 检查 `WQ_USERNAME` 和 `WQ_PASSWORD` 配置
- 确认账号有API访问权限

### 2. 数据库连接失败
- 确保MySQL服务正在运行
- 检查数据库配置参数

### 3. 回测结果为空
- 验证Alpha表达式有效性
- 检查数据库连接和权限

### 4. 邮件通知未发送
- 验证SMTP配置参数
- 检查网络连接和防火墙设置

## 性能优化建议

1. **并发设置**: 根据系统资源调整 `max_workers`
2. **批量处理**: 使用pipeline命令批量处理多个模板
3. **缓存利用**: 数据字段会自动缓存到本地
4. **断点续传**: 长时间任务支持中断恢复

## 监控和日志

- 日志级别通过 `LOG_LEVEL` 环境变量控制
- 支持DEBUG、INFO、WARNING、ERROR等级别
- 重要操作会记录详细日志

## 数据安全

- 敏感信息存储在 `.env` 文件中
- 建议将 `.env` 文件添加到 `.gitignore`
- 定期备份数据库和重要结果文件

## 技术支持

如遇到问题，请检查：
1. 日志文件中的错误信息
2. 网络连接状态
3. 配置文件格式
4. 依赖包版本兼容性

## 更新日志

请参考项目README文件了解最新更新内容。