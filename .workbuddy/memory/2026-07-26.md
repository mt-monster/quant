# 2026-07-26 工作日志

## 回测进度监控（10:45 报告）
- 产出 `worldquant_alpha/deliverables/reports/backtest_monitor_report_20260726_1045.md`
- 机器级枚举 28 个 python 进程：主账号 ds 舰队 6 路在飞(scan_tri_job) + 1 rescue(scan_rescue_tvr, dl_riskfree_returns) + 1 keeper(target=6)；6 个 cnhkmcp MCP 宿主；独立项目 D:\BaiduNetdiskDownload\...(tabbit/option9) 8 进程。
- 🔴 **根因发现**：ds 舰队整体 ~100% 提交报错(status=error,pid=null)，`fundamental65`(38896) 与 `chart_cnn_alpha`(51492) 进程已死。根因 = WinError 32 文件锁冲突——多进程并发写共享状态文件 `results/.brain_sim_submit_gate.json`，`os.replace` 原子 rename 在 Windows 下被其他进程占用而失败。另 rescue + chart_cnn 触发 429 风暴(退避至 120s)。
- ds 舰队 23 数据集 5440 变体 0 候选；scan_v 系列 25 任务 5787 变体 42 个 PASS_CHEAP 候选(v52b=28/v39b=10/v52=4)。`manual_submit_ready.json` 仅 1 条且 submitted=False → **0 真正提交**。
- 建议：先修 submit_gate 跨进程文件锁(WinError 32)再重开 8 路；降 rescue 速率避 429 互挤；v52b 候选汇总进 manual_submit_ready 供人工提交。

## 监控方法固化
- 本报告手工核编（进程枚举+checkpoint 实算），比自动 `build_md_report.py` 更准——自动工具漏报死亡作业与文件锁根因。
- PowerShell 工具在本环境不回显 stdout，改用 `Set-Content` 写文件再 Read 才可靠；Bash 内调用 powershell.exe 被安全策略拦截，须用 PowerShell 工具本身。
