基于提供的算子列表和数据集信息，以下设计几个具有明确经济逻辑的Alpha模板。这些模板遵循“数据清洗 → 时序/横截面变换 → 分组/中性化 → 排序/衰减”的结构化流程，并留出关键参数作为可填充的“插槽”。

---

### 模板 1：行业中性化的残差动量 (CAPM残差扩展)
**经济逻辑**：剥离股票收益率中的行业共性部分，捕捉纯粹的个股特异性动量。这避免了行业轮动带来的噪音，专注于公司自身动量的延续性。
**模板结构**：
1.  **数据清洗**：`winsorize(ts_backfill(returns, 63), std=4)`
    *   `returns`: 插槽，可替换为其他目标变量（如波动率、交易量变化）。
2.  **计算行业基准**：`group_mean(清洗后的returns, log(ts_mean(cap, 21)), sector)`
    *   `sector`: 插槽，可替换为 `industry`, `country` 或其他分组字段。
    *   使用市值对数平滑值作为权重，减少大市值股在行业均值中的过度影响。
3.  **提取残差**：`ts_regression(清洗后的returns, 行业基准, 252, rettype=0)`
    *   `252`: 插槽，回归窗口期。
    *   `rettype=0`: 返回残差。
4.  **横截面排序**：`rank(残差)`
5.  **(可选) 行业中性化**：`group_neutralize(排序后的值, sector)`
    *   进行二次中性化，确保因子在行业间完全中性。
6.  **(可选) 衰减调仓**：`ts_decay_linear(最终因子值, 5)` 或 `hump(最终因子值, ...)`
    *   控制换手率，使信号平滑。

---

### 模板 2：分析师预期修正的陡度 (预期曲线结构)
**经济逻辑**：比较不同预测期（如当前财年 vs 下一财年）的分析师预期均值。近端预期上调幅度远超远端，可能预示基本面加速改善，是强烈的正向信号。
**模板结构**：
1.  **数据选择与清洗**：
    *   `near_term_eps = winsorize(ts_backfill(anl14_mean_eps_fy1, 63), std=3)`
    *   `far_term_eps = winsorize(ts_backfill(anl14_mean_eps_fy2, 63), std=3)`
    *   `fy1`, `fy2`: 插槽，可替换为 `fp1`, `fp2` (财务周期) 或其他预期字段。
2.  **计算预期差**：`subtract(near_term_eps, far_term_eps)`
3.  **行业内标准化**：`group_zscore(预期差, industry)`
    *   `industry`: 插槽，可替换为 `sector`。
    *   消除行业间的整体预期水平差异，聚焦于行业内公司的相对排序。
4.  **二次横截面标准化**：`zscore(行业内标准化后的值)`
    *   确保最终因子在全市场横截面上具有统一的尺度。
5.  **(可选) 条件交易**：`trade_when(最终因子值, ts_rank(volume, 20) > 0.3, NaN)`
    *   仅在过去20日成交量排名前30%的股票上产生信号，规避流动性不足的股票。

---

### 模板 3：价量背离的隐性强度 (量价确认)
**经济逻辑**：价格创新高但成交量并未同步放大，可能意味着上涨动力不足；反之，价格小幅上涨伴随成交量急剧放大，则可能显示有资金暗中吸筹，后续潜力较大。本模板捕捉后一种情况。
**模板结构**：
1.  **价格动量计算**：`ts_returns(close, 5)`  // 5日价格收益率
    *   `5`: 插槽，价格周期。
2.  **成交量动量计算**：`ts_returns(volume, 5)`  // 5日成交量增长率
    *   `5`: 插槽，成交量周期。
3.  **计算价量比**：`divide(价格动量, ts_zscore(成交量动量, 10))`
    *   `10`: 插槽，z-score计算窗口。
    *   用成交量动量的z-score进行标准化，以识别“异常”的价量关系。
4.  **横截面排序**：`rank(价量比)`
5.  **市值中性化**：`vector_neut(排序后的值, log(cap))`
    *   消除因子与市值的相关性。
6.  **(可选) 去极值**：`tail(中性化后的值, -3, 3, NaN)`
    *   将因子值超出±3倍标准差的极端值设为NaN，避免个别股票对组合的过度影响。

---

### 模板 4：多因子模型残差的横截面挖掘 (集成模型残差)
**经济逻辑**：利用提供的 `model110` 集成因子（如 `mdl110_score`），但其预测可能在全市场层面存在系统性偏差。计算该因子与其自身在不同分组（如行业）内均值的残差，可以挖掘出模型在组内相对低估或高估的股票。
**模板结构**：
1.  **选择模型因子**：`winsorize(ts_backfill(mdl110_score, 21), std=4)`
    *   `mdl110_score`: 插槽，可替换为 `mdl110_quality`, `mdl110_growth` 等任一子因子。
2.  **计算组内基准**：`group_mean(清洗后的模型因子, 1, sector)`  // 等权组内均值
    *   `sector`: 插槽，分组字段。
    *   `1`: 表示等权，此处为插槽，也可替换为市值权重向量。
3.  **计算组内残差**：`subtract(清洗后的模型因子, 组内基准)`
4.  **时序动量增强**：`ts_mean(组内残差, 10)`
    *   `10`: 插槽，动量窗口。取残差的短期均值，使信号更稳定。
5.  **横截面排序**：`rank(动量增强后的残差)`
6.  **(可选) 目标换手率衰减**：`ts_target_tvr_decay(排序后的值, target_tvr=0.15, lambda_min=0.8, lambda_max=1.2)`
    *   自动调整衰减参数，将因子换手率精准控制在大约15%的目标水平。

---

### 模板 5：期权隐含偏度的跨品种比较 (期权市场信息)
**经济逻辑**：同一标的的看涨期权与看跌期权的隐含波动率（或Delta、Vega）之差，反映了期权市场对未来方向性风险的定价。通过比较不同股票间的这个差值，可以发现市场对某些股票存在异常乐观或悲观的情绪偏差。
**模板结构**：
1.  **计算期权希腊值差**：`subtract(put_iv, call_iv)` 或 `subtract(put_delta, call_delta)`
    *   `put_iv`, `call_iv` 等: 插槽，具体的期权数据字段。
2.  **行业中性化**：`group_neutralize(希腊值差, industry)`
3.  **时序稳定性过滤**：`if_else(ts_std_dev(希腊值差, 20) < 0.5, 行业中性化后的值, NaN)`
    *   `20`, `0.5`: 插槽，窗口期和阈值。
    *   剔除过去波动过大的信号，保留相对稳定的情绪指标。
4.  **横截面排序**：`rank(过滤后的值)`
5.  **(可选) 与历史位置比较**：`subtract(当前rank值, ts_delay(rank值, 5))`
    *   最终信号不是绝对水平，而是近期排名的变化，捕捉情绪的动态转折。

---

**使用说明**：
*   **插槽**：模板中标注为“插槽”的部分，是策略研究员需要根据具体数据字段、参数偏好和风险目标进行填充和实验的地方。
*   **模块化**：每个步骤（如清洗、分组、中性化）可以独立调整或替换，鼓励复用。
*   **经济逻辑第一**：填充插槽时，应始终考虑所选数据和参数的经济含义，避免无意义的过拟合。这些模板提供了系统化搜索的起点，但最终的有效性需通过严格的回测验证。