# V34 insider_matrix PPA 挖掘进度汇报

**时间**: 2026-07-23 15:36 GMT+8
**数据集**: USA / TOP1500 / insider_matrix (eur_aggregated_value 系列)
**最新日志**: `results/v34_progress_20260723_152946.log` (另有早期 `v34_progress_20260723_141747.log`)

---

## 1. 运行状态：扫描已完成，无需重启

- 进度日志最终事件：`done = 324 / total = 324`（**100% 完成**），ETA `2026-07-23 15:33:42`。
- 进程检查（PowerShell `Get-CimInstance` + `ps`）：**当前无任何 `scan_v34` 进程在运行**。
- 判定：扫描是**自然跑完退出**，并非中途崩溃。因此**无需重启扫描**。
- 最终输出 `results/scan_v34_insider_20260723_153342.json` 内容为 `[]`（该文件仅存放通过全部闸门的 `found_alphas`，为空即无候选，符合预期）。

## 2. 结果总览

从进度日志 `recent` 窗口（保留最近 8 步，共可追溯 121 个变体最终状态）解析：

| 类别 | 数量 | 说明 |
|------|------|------|
| 产出 sharpe 且 FAIL | 33 | 全部 `fails=['platform_FAIL']` |
| backtest 报错 (error) | 88 | 回测请求异常 |
| **PASS 候选** | **0** | 无候选 → 未提交任何 alpha（符合规则，本自动化不自动提交） |

> 注：剩余 203 个变体因滑出 `recent` 窗口无法从日志重建（见第 5 节数据完整性告警）。

## 3. 关键发现：`platform_FAIL` 疑似误判，11 个高指标变体漏网

33 个 FAIL 变体中，有 **11 个（7 个唯一 PID）** 指标优秀却被标 `platform_FAIL`：

- **IS_LADDER 全部 PASS**（1.71 ~ 2.17）
- **Sharpe 1.73 ~ 1.95**，**Fitness 1.13 ~ 1.36**
- **Margin 11.4 ~ 15.7 bp**（> 10bp 闸门），**TVR 7.4% ~ 10.7%**（在 5–30% 区间）
- 廉价数值闸门（S/F/TVR/margin/returns）**本应全部通过**

`platform_FAIL` 来自 `check_alpha_gates()` 返回 `passes_cheap=False`。代码逻辑（`scan_v34_insider_matrix.py:105`）显示：`check_alpha_gates` 仅在 `/check` 接口拉取返回 `None` 时才返回 `(False, {})`；而日志中这些变体的 `checks={}`、`failed_checks=None`，**没有记录到任何具体失败的平台闸门名**。

**结论**：这批强信号 alpha 极可能是因平台检查 API 拉取失败（`ch is None`）被**误判 FAIL**，而非真正被某道平台闸门拒绝。IS_LADDER 本身全部 PASS 佐证了这一点。

## 4. 真正失败的变体（非误判）

- **`delta_reversal_eur_aggregated_value_*` 系列**（d3–d6）：IS_LADDER **FAIL**，Fitness 仅 0.54–0.74，TVR ~29–31%（超出 30% 上限），margin < 10bp —— 真实弱信号。
- **`rank_momentum_eur_aggregated_value_d5`**：Fitness 0.47 —— 真实不达标。

## 5. 数据完整性告警（需修复）

- **断点续跑 checkpoint 文件 `results/v34_insider_matrix_checkpoint.json` 缺失**。`save_checkpoint()` 采用原子写（`tmp` + `os.replace`），失败仅记 `logger.warning` 不抛异常，因此静默丢失。
- 后果：全 324 变体无法从 checkpoint 完整重建；若再次运行且 `load_checkpoint()` 返回 `None`，将**当作全新扫描从头跑**（浪费约 324 步）。
- 建议：检查 `save_checkpoint` 写权限/异常根因；当前结果仅能依靠进度日志 `recent` 窗口（121 个）追查。

## 6. 候选列表（疑似漏网，待人工复核，**未提交**）

| 唯一 PID | Label（代表） | S | F | TVR | Margin | IS_LADDER |
|----------|--------------|-----|-----|------|--------|-----------|
| `e7xn7ng6` | ind_momentum_eur_aggregated_value_d3_w0.35 | 1.950 | 1.360 | 10.7% | 11.4bp | PASS |
| `QP9nZvXQ` | mom_reversal_eur_aggregated_value_d3 (w0.3/0.35/0.4) | 1.900 | 1.320 | 7.7% | 15.7bp | PASS |
| `ZYKjYOgY` | momentum_eur_aggregated_value_d3_w0.35 | 1.880 | 1.290 | 7.7% | 15.4bp | PASS |
| `zqR5q71E` | ind_momentum_eur_aggregated_value_d4_w0.35 | 1.820 | 1.220 | 9.9% | 11.4bp | PASS |
| `pwKnmmpv` | mom_reversal_eur_aggregated_value_d4 (w0.3/0.35/0.4) | 1.800 | 1.210 | 7.4% | 15.3bp | PASS |
| `mLVqLOk2` | momentum_eur_aggregated_value_d4_w0.35 | 1.770 | 1.180 | 7.4% | 15.1bp | PASS |
| `wpE5p2zQ` | ind_momentum_eur_aggregated_value_d5_w0.35 | 1.730 | 1.130 | 9.3% | 11.6bp | PASS |

> 上表 7 个 PID 对应 11 个 FAIL 记录（部分 PID 被多个 weight 变体复用，因表达式近似被 WQ 判为同一 alpha）。**请到 WQ 后台对这些 PID 直接执行 `/check` 复核**，确认是否真有平台闸门 FAIL；若实际全过，则可手动提交。

## 7. 建议下一步

1. **人工复核上述 7 个 PID** 的平台闸门（最高优先级，可能直接产出可提交候选）。
2. **修复 `check_alpha_gates` 对 `ch is None` 的处理**：API 拉取失败时计入"待复核"而非直接判 FAIL，并加重试/退避。
3. **修复 `save_checkpoint` 持久化**：排查写失败根因，确保续跑 checkpoint 落地。
4. 若需重跑闸门检查，建议针对 33 个 FAIL 的 PID 单独重跑闸门逻辑（而非重跑全量 324 步）。

---
*本报告由自动化任务生成，未提交任何 alpha。*
