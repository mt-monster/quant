# 2026-07-14

## USA/D1 PPA 挖掘：提交闸门全面硬化
- 用户要求：未达硬性标准的 alpha 一律不得提交至平台。
- 重构 `mine_usa_ppa_multi.py` 的提交闸门为两段式：
  - **廉价闸门 `check_cheap_gates`**（PC 等待前判定，省配额）：Sharpe≥1.58、Fitness≥1.00（=平台 LOW_SHARPE/LOW_FITNESS 限额）、Margin>5bp、TVR∈[5%,20%]、Returns>5%、Returns>Drawdown、平台检查无 FAIL。
  - **最终硬闸门 `evaluate_hard_gates`**（PC 等待后、提交前）：生产相关性必须出来且 <0.70（绝对红线）、自相关性 SELF_CORRELATION<0.50（Stage5.1，须已算出）、复校廉价闸门。
  - `submit_one` 内再加一道防御性终审，任何一项不达标绝不设 `color=GREEN`/tags。
- 用 10 个合成案例做功能测试，accept/reject 全部符合预期。
- 已停旧进程、重建 `wd_lib_wrapper.py`、重启挖掘（后台 task TUd2Xo，日志 mining_multi.log）。
- 当前约束常量：MAX_PROD_CORR=0.70、SELF_CORR_MAX=0.50、PRE_SHARPE=1.58、PRE_FITNESS=1.00、HARD_MARGIN_BP=5、HARD_TVR_MIN=0.05、HARD_TVR_MAX=0.20、HARD_RETURNS=0.05。
- 并发：每数据集 3 线程×2=6，信号量锁 5 在飞（服务端并发上限 C=5，之前实测）。

## 挖掘有效性审计（对照 workflow.md）
- 日志统计：34 条已回测，Sharpe 中位 0.09、max 1.56，**0 条通过硬闸门**（需 ≥1.58）。
- 结构性缺陷：institutions20/18 日频字段上 `rank(ts_zscore(subtract/divide))` 范式 → TVR 21–52%（100% 越界，需 5–20%）、Margin <3bp（100% 不达标）。
- 流程缺失：**Stage 4（decay/neut/窗口参数优化）完全未实现**——这是 workflow 把边界候选调优成可提交 alpha 的核心环节。Stage 3.3 快速失败（连续 32 条 Sharpe<0.7 切数据集）也未实现，脚本仍在已判死数据集上无限重循环。
- 最佳候选 `i20 #3`（S=1.56, TVR=47.9%）被直接丢弃——本应进 Stage 4 decay 扫描+加 hump 压换手抢救。
- 速度瓶颈：服务端模拟延迟（中位 79s/条）+ 429 限流，C=5 不可突破；根本瓶颈是 0% 命中率。
- 建议（已写入 AUDIT_mining_effectiveness.md）：P0 换低换手数据集(fundamentals/analyst)+抢救 S=1.56 候选；P1 改用 ts_rank/hump/ts_mean 降换手段式；P2 本地 TVR 预筛省配额；P3 自适应学习式淘汰。
- 关键认知：当前不是「慢」是「方向错」，提速无效，须换数据集+落地 Stage 4。

## 审计后重构重跑（P0+P1 落地）
- **数据集切换**：institutions20/18 → institutions6（22 MATRIX 日频）+ fund_holdings_panel（30 VECTOR 季频）。
  - 发现 USA/D1/TOP3000 仅 5 个未点亮数据集，全是 institutions 类——无 fundamentals/analyst 未点亮可用。
  - fund_holdings_panel 全是 VECTOR 字段（herfindahl/holding_value/transaction 等），用 vec_avg 聚合后 ts_backfill，季频天然低换手。
- **表达式族重构（P1 核心）**：build_exprs_for_pair 全部改为降换手优先：
  - ts_rank(126/252) 替代 ts_zscore(66)（长窗口稳定排名）
  - hump(expr, hump=0.01) 压换手（注意：hump 的阈值是命名参数 `hump=`，不是位置参数！`hump(x, 0.01)` 会报 "Invalid number of inputs: 2"）
  - ts_mean(spread, 22) 平滑降噪
  - ts_corr/ts_regression 关系类（天然低换手）
  - ts_quantile(x, 126)（注意：第三参数是 driver="gaussian"，不是数字）
- **Stage 4 参数扫描**（workflow Stage 4）：`stage4_sweep()` 函数，当 Sharpe≥0.9 但 TVR>20% 时自动触发，生成 decay{8,16,24}/hump/ts_rank 变体逐条回测。
- **Stage 3.3 快速失败**：`_ds_stats` + `_check_fast_fail()`，连续 32 条最高 Sharpe<0.7 → PROBABLE_FAIL 切换数据集。
- **Stage 4 抢救脚本** `rescue_stage4.py`：对 S=1.56 候选做 16 条变体并行扫描，但 WQ 集群拥堵（全卡 35%）未跑完。Stage 4 逻辑已集成进主挖掘脚本。
- **WQ 算子语法关键**：hump(x, hump=0.01)、ts_quantile(x, d, driver="gaussian")——命名参数不能省略前缀。
- 新挖掘已启动（task oSLVvh，日志 mining_v2.log），institutions6 + fund_holdings_panel 并行，降换手段式 + Stage 4 + Stage 3.3 + 硬闸门全开。

## 401 自动重认证修复 + mining v2 结果分析
- mining v2 跑了 ~7.5h，凌晨 04:32 后会话过期，累计 4847 次 401 错误。
- **修复 `wd_lib_wrapper.py`**：新增 `_reauth()` 方法（线程安全，带锁），在 `run_backtest` 提交/轮询遇 401 时自动重认证并重试（不消耗 attempt 配额）；`get_alpha_details`/`get_alpha_check` 也加了 401 重试（最多 2 次）。
- mining v2 结果（47 条回测）：Sharpe max=0.54（全部 <0.7），TVR 分布改善明显——53% 在 5-20% 范围内（之前 0%），18% 过低(<5%，hump 过度压换手)，9% 过高(>20%，ts_corr 类)。
- 降换手段式生效但信号偏弱：institutions6/fund_holdings_panel 字段组合的 Sharpe 普遍低，需更多探索或换字段对。
- mining v3 已启动（task z8l06j，日志 mining_v3.log），带 401 自动重认证。

## Mining v3 结果 + Stage 4 抢救全流程
- Mining v3 完成（12:34）：98 条回测，0 通过，最高 S=0.600。i6 和 fhp 都触发 Stage 3.3 快速失败。
- **Stage 4 抢救 V2**（rescue_stage4.py，修复 hump 语法 bug 后重跑）：
  - 26 条变体对 S=1.56 候选（institutions20 inst20_ra_sev/sv）做参数扫描。
  - **decay=32 是甜点**：d32 S=1.780 F=1.010 TVR=17.6% M=6.4bp（廉价闸门全过）；tsmean_d32 S=1.730 F=1.110 TVR=9.1% M=11.3bp。
  - **但两个候选 PC 等待超时**（WQ 服务端拥堵，1 小时未出 PROD_CORRELATION）。
  - tsmean44_d32（ts_mean 44 日）S=1.890 F=1.220 TVR=8.1% M=12.7bp — 更长平滑窗口显著提高信号。
- **直接查 API 发现三个不可修复的 FAIL**：
  - CONCENTRATED_WEIGHT=0.5（需 <0.1）— inst20_ra_sev/sv 字段覆盖度低，权重集中在少数股票。truncation 0.01-0.04 完全不影响此值。
  - LOW_2Y_SHARPE=0.0（需 ≥1.58）— 信号在近 2 年完全失效。
  - LOW_DURATION=308-351（需 ≥500）— 有效回测天数不足。testPeriod P5Y→P6Y 无变化（平台最大 P6Y）。
- **Stage 4 抢救 V3**（rescue_stage4_v3.py）：11 条变体（truncation 0.01-0.04 + testPeriod P6Y + decay 32-48 + ts_mean 22/44）。全部 FAIL，CW/2Y/Dur 三项不可修复。
  - 最佳：tsmean_d48 S=1.900 F=1.270 TVR=8.3% M=13.4bp — 指标极好但被三个 FAIL 否决。
- **关键认知**：institutions20 的 inst20_ra_sev/sv 字段对是死路。信号质量足够（S=1.9, F=1.27），但数据覆盖度不足导致平台检查不可通过。需要换到覆盖度更高的字段对或数据集。
- **testPeriod 平台限制**：最大 P6Y0M0D（6年），P10Y 会报 400。
- **hump 语法**：`hump(x, hump=0.01)` 命名参数，`hump(x, 0.01)` 位置参数报 "Invalid number of inputs: 2"。
